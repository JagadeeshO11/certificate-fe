# Codeathon 2K26 Frontend

React + Vite frontend for the certificate mail dashboard.

## Run

```powershell
npm install
npm run dev
```

The app runs on `http://localhost:5173` and expects the backend API at `http://localhost:5000`.
